---
# yaml-language-server: $schema=https://kubernetes-schemas.ergho.org/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: lldap
  namespace: authentication
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 2.0.3
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      interval: 15m
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    initContainers:
      01-init-db:
        image: ghcr.io/onedr0p/postgres-init:14.9
        imagePullPolicy: IfNotPresent
        envFrom:
          - secretRef:
              name: lldap-secret
    controller:
      type: statefulset
    image:
      repository: nitnelave/lldap
      tag: v0.5.0-alpine@sha256:7df30065344c5b7f73bae2591db779460ae937e39e11b4a5d1adeb91082ed97e
    service:
      main:
        ports:
          http:
            port: 3890
          web:
            enabled: true
            protocol: TCP
            port: 17170
    ingress:
      main:
        enabled: true
        ingressClassName: internal
        hosts:
          - host: &host "lldap.ergho.org"
            paths:
              - path: /
                pathType: Prefix
                service:
                  port: 17170
        tls:
          - hosts:
              - *host
    volumeClaimTemplates:
      - name: config
        mountPath: /data
        accessMode: ReadWriteOnce
        size: 1Gi
        storageClass: ceph-block
    envFrom:
      - secretRef:
          name: lldap-secret
    env:
      TZ: "Europe/Stockholm"
      UID: "568"
      GID: "568"
