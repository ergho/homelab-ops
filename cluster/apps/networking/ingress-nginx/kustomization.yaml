---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
configMapGenerator:
  - name: cloudlfare-proxied-networks
    files:
      - ./cloudlfare-proxied-networks.txt
generatorOptions:
  diableNameSuffixHash: true
