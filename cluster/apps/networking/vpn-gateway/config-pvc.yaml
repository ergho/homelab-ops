---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pod-gateway-config
  namespace: vpn-gateway
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: ceph-block
