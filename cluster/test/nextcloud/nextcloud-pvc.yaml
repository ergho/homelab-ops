---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    volume.beta.kuernetes.io/storage-class: iscsi-csi
  labels:
    app: nextcloud-server
  name: iscsi-pvc-nextcloud
  namespace: nextcloud
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: iscsi-csi
