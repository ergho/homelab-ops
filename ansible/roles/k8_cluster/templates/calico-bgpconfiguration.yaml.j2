---
apiVersion: crd.projectcalico.org/v1
kind: BGPConfiguration
metadata:
  name: default
spec:
  asNumber: {{ calico_bgp_as_number }}
  nodeToNodeMeshEnabled: true
  serviceClusterIPs:
    - cidr: "{{ k8s_service_network }}"
  serviceExternalIPs:
    - cidr: "{{ calico_bgp_external_ips }}"
